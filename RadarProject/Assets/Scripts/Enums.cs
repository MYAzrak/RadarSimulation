public enum ShipType
{
    GasCarrier,
    TugBoat,
    Container,
    Cargo,
}

public enum Waves
{
    Calm,
    Moderate,
}

// Whether we want to see the NoiseMap, ColorMap or Mesh
public enum DrawMode
{
    NoiseMap,
    ColorMap,
    Mesh,
};